---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: softwarefactories.sf.softwarefactory-project.io
spec:
  group: sf.softwarefactory-project.io
  names:
    kind: SoftwareFactory
    listKind: SoftwareFactoryList
    plural: softwarefactories
    shortNames:
    - sf
    singular: softwarefactory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Status
      jsonPath: .status.conditions[0].status
      name: Status
      type: string
    - description: Message
      jsonPath: .status.conditions[0].message
      name: Message
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: SoftwareFactory is the Schema for the softwarefactories API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SoftwareFactorySpec defines the desired state of SoftwareFactory
            properties:
              config-location:
                description: Config repository spec
                properties:
                  base-url:
                    description: Base URL to use to perform git-related actions on
                      the config repository. For example, if hosted on GitHub, the
                      Base URL would be `https://github.com/<username>/`
                    pattern: ^https?:\/\/.+$
                    type: string
                  name:
                    description: The name of the `config` repository. This value is
                      appended to `base-url` to clone the repository
                    minLength: 1
                    type: string
                  zuul-connection-name:
                    description: Name of the Zuul connection through which Zuul can
                      handle git events on the config repository
                    minLength: 1
                    type: string
                required:
                - base-url
                - name
                - zuul-connection-name
                type: object
              fqdn:
                description: The fully qualified domain name to use with the deployment.
                  Relevant services will be served at https://`service`.`FQDN`
                type: string
              gitserver:
                description: Git server spec
                properties:
                  storage:
                    properties:
                      className:
                        description: Default storage class to use with Persistent
                          Volume Claims issued by this resource. Consult your cluster's
                          configuration to see what storage classes are available
                          and recommended for your use case.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: 'Storage space to allocate to the resource, expressed
                          as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                type: object
              letsEncrypt:
                description: LetsEncrypt settings for enabling using LetsEncrypt for
                  Routes/TLS
                properties:
                  server:
                    description: 'Specify the Lets encrypt server. Valid values are:
                      - "staging" - "prod"'
                    enum:
                    - prod
                    - staging
                    type: string
                required:
                - server
                type: object
              logserver:
                description: Logserver service spec
                properties:
                  loopDelay:
                    description: The frequency, in seconds, at which the log pruning
                      cronjob is running. Defaults to 3600s, i.e. logs are checked
                      for pruning every hour
                    type: integer
                  retentionDays:
                    description: Logs retention time in days. Logs older than this
                      setting in days will be purged by a pruning cronjob. Defaults
                      to 60 days
                    type: integer
                  storage:
                    description: Storage-related settings
                    properties:
                      className:
                        description: Default storage class to use with Persistent
                          Volume Claims issued by this resource. Consult your cluster's
                          configuration to see what storage classes are available
                          and recommended for your use case.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: 'Storage space to allocate to the resource, expressed
                          as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                type: object
                x-kubernetes-validations:
                - message: retentionDays must be a positive integer if set
                  rule: '!has(self.retentionDays) || self.retentionDays > 0'
                - message: loopDelay must be a positive integer if set
                  rule: '!has(self.loopDelay) || self.loopDelay > 0'
              mariadb:
                description: MariaDB service spec
                properties:
                  dbStorage:
                    description: Storage parameters related to mariaDB's data
                    properties:
                      className:
                        description: Default storage class to use with Persistent
                          Volume Claims issued by this resource. Consult your cluster's
                          configuration to see what storage classes are available
                          and recommended for your use case.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: 'Storage space to allocate to the resource, expressed
                          as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  logStorage:
                    description: Storage parameters related to the database's logging
                    properties:
                      className:
                        description: Default storage class to use with Persistent
                          Volume Claims issued by this resource. Consult your cluster's
                          configuration to see what storage classes are available
                          and recommended for your use case.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: 'Storage space to allocate to the resource, expressed
                          as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                required:
                - dbStorage
                - logStorage
                type: object
              nodepool:
                description: Nodepool services spec
                properties:
                  builder:
                    description: Nodepool-builder related settings
                    properties:
                      logLevel:
                        description: 'Specify the Log Level of the nodepool launcher
                          process. Valid values are: - "INFO" (default) - "WARN" -
                          "DEBUG"'
                        enum:
                        - INFO
                        - WARN
                        - DEBUG
                        type: string
                      storage:
                        description: Storage related settings
                        properties:
                          className:
                            description: Default storage class to use with Persistent
                              Volume Claims issued by this resource. Consult your
                              cluster's configuration to see what storage classes
                              are available and recommended for your use case.
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: 'Storage space to allocate to the resource,
                              expressed as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - size
                        type: object
                    type: object
                  launcher:
                    description: Nodepool-launcher related settings
                    properties:
                      logLevel:
                        description: 'Specify the Log Level of the nodepool launcher
                          service. Valid values are: - "INFO" (default) - "WARN" -
                          "DEBUG" Changing this value will restart the service.'
                        enum:
                        - INFO
                        - WARN
                        - DEBUG
                        type: string
                    type: object
                  statsdTarget:
                    description: The address to forward statsd metrics to (optional),
                      in the form "host:port"
                    type: string
                type: object
              storageClassName:
                description: Default storage class to use by Persistent Volume Claims
                type: string
              zookeeper:
                description: Zookeeper service spec
                properties:
                  storage:
                    properties:
                      className:
                        description: Default storage class to use with Persistent
                          Volume Claims issued by this resource. Consult your cluster's
                          configuration to see what storage classes are available
                          and recommended for your use case.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: 'Storage space to allocate to the resource, expressed
                          as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                required:
                - storage
                type: object
              zuul:
                description: Zuul service spec
                properties:
                  defaultAuthenticator:
                    description: The name of the default authenticator to use if no
                      authenticator is bound explicitly to a tenant with zuul-web
                    type: string
                  executor:
                    description: Configuration of the executor microservices
                    properties:
                      logLevel:
                        description: 'Specify the Log Level of the zuul-executor service.
                          Valid values are: - "INFO" (default) - "WARN" - "DEBUG"
                          Changing this value will restart the service.'
                        enum:
                        - INFO
                        - WARN
                        - DEBUG
                        type: string
                      replicas:
                        description: How many executor pods to run
                        format: int32
                        type: integer
                      storage:
                        description: Storage-related settings
                        properties:
                          className:
                            description: Default storage class to use with Persistent
                              Volume Claims issued by this resource. Consult your
                              cluster's configuration to see what storage classes
                              are available and recommended for your use case.
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: 'Storage space to allocate to the resource,
                              expressed as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - size
                        type: object
                    type: object
                  gerritconns:
                    description: The list of Gerrit-based connections to add to Zuul's
                      configuration
                    items:
                      description: 'Describes a Zuul connection using the `gerrit`
                        driver: https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#connection-configuration'
                      properties:
                        canonicalhostname:
                          description: The canonical hostname associated with the
                            git repositories on the Gerrit server. Equivalent to https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20connection%3E.canonical_hostname
                          type: string
                        git-over-ssh:
                          default: false
                          description: Set to true to force git operations over SSH
                            even if the password attribute is set. Equivalent to https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20ssh%20connection%3E.git_over_ssh
                          type: boolean
                        hostname:
                          description: The gerrit server hostname. Equivalent to https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20connection%3E.server
                          type: string
                        name:
                          description: How the connection will be named in Zuul's
                            configuration and appear in zuul-web
                          type: string
                        password:
                          description: The name of a Kubernetes secret holding the
                            Gerrit user's API Password. The secret's data must have
                            a key called "password". Equivalent to https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20ssh%20connection%3E.password
                          type: string
                        port:
                          default: 29418
                          description: SSH port number to the Gerrit instance. Equivalent
                            to https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20ssh%20connection%3E.port
                          type: integer
                        puburl:
                          description: URL to Gerrit's web interface. https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20connection%3E.baseurl
                          pattern: ^https?:\/\/.+$
                          type: string
                        username:
                          default: zuul
                          description: Username that Zuul will use to authenticate
                            on the Gerrit instance. Equivalent to https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20connection%3E.user
                          type: string
                        verifyssl:
                          default: true
                          description: Disable SSL certificate verification with the
                            Gerrit instance when set to false. Equivalent to https://zuul-ci.org/docs/zuul/latest/drivers/gerrit.html#attr-%3Cgerrit%20ssh%20connection%3E.verify_ssl
                          type: boolean
                      required:
                      - hostname
                      - name
                      type: object
                    type: array
                  githubconns:
                    description: The list of GitHub-based connections to add to Zuul's
                      configuration
                    items:
                      description: 'Describes a Zuul connection using the `github`
                        driver: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#'
                      properties:
                        apiToken:
                          description: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#attr-%3Cgithub%20connection%3E.api_token
                          type: string
                        appId:
                          description: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#attr-%3Cgithub%20connection%3E.app_id
                          type: string
                        appKey:
                          description: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#attr-%3Cgithub%20connection%3E.app_key
                          type: string
                        canonicalHostname:
                          description: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#attr-%3Cgithub%20connection%3E.canonical_hostname
                          type: string
                        name:
                          description: How the connection will be named in Zuul's
                            configuration and appear in zuul-web
                          type: string
                        server:
                          description: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#attr-%3Cgithub%20connection%3E.server
                          type: string
                        verifySsl:
                          default: true
                          description: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#attr-%3Cgithub%20connection%3E.verify_ssl
                          type: boolean
                        webHookToken:
                          description: https://zuul-ci.org/docs/zuul/latest/drivers/github.html#attr-%3Cgithub%20connection%3E.webhook_token
                          type: string
                      required:
                      - apiToken
                      - appId
                      - appKey
                      - name
                      type: object
                    type: array
                  merger:
                    description: Configuration of the merger microservice
                    properties:
                      gitHttpLowSpeedLimit:
                        description: https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-merger.git_http_low_speed_limit
                        format: int32
                        minimum: 0
                        type: integer
                      gitHttpLowSpeedTime:
                        description: https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-merger.git_http_low_speed_time
                        format: int32
                        minimum: 0
                        type: integer
                      gitTimeout:
                        description: https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-merger.git_timeout
                        format: int32
                        minimum: 1
                        type: integer
                      gitUserEmail:
                        description: https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-merger.git_user_email
                        type: string
                      gitUserName:
                        description: https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-merger.git_user_name
                        type: string
                      logLevel:
                        description: 'Specify the Log Level of the nodepool launcher
                          service. Valid values are: - "INFO" (default) - "WARN" -
                          "DEBUG" Changing this value will restart the service.'
                        enum:
                        - INFO
                        - WARN
                        - DEBUG
                        type: string
                      minReplicas:
                        default: 1
                        description: How many merger pods to run
                        format: int32
                        type: integer
                      storage:
                        description: Storage-related settings
                        properties:
                          className:
                            description: Default storage class to use with Persistent
                              Volume Claims issued by this resource. Consult your
                              cluster's configuration to see what storage classes
                              are available and recommended for your use case.
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: 'Storage space to allocate to the resource,
                              expressed as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - size
                        type: object
                    type: object
                  oidcAuthenticators:
                    description: A list of OpenID Connect authenticators that will
                      enable admin API access on zuul-web
                    items:
                      description: The description of an OpenIDConnect authenticator,
                        see https://zuul-ci.org/docs/zuul/latest/configuration.html#authentication
                      properties:
                        audience:
                          description: Optionally provide the claim where the audience
                            is set if not in `aud`, equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-audience
                          type: string
                        authority:
                          description: Optionally provide the claim where the authority
                            is set if not in `iss`, equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-authority
                          type: string
                        clientID:
                          description: The client ID, as exposed in the `aud` claim
                            of a JWT. Equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-auth%20%3Cauthenticator%20name%3E.client_id
                          type: string
                        issuerID:
                          description: The issuer ID, as exposed in the `iss` claim
                            of a JWT. Equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-auth%20%3Cauthenticator%20name%3E.issuer_id
                          type: string
                        keysURL:
                          description: Optionally provide a URL to fetch the Identity
                            Provider's key set, equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-keys_url
                          type: string
                        loadUserInfo:
                          default: true
                          description: If set to false, zuul-web will skip loading
                            the Identity Provider's `userinfo` endpoint and rely on
                            what's available in the JWT. Equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-load_user_info
                          type: boolean
                        maxValidityTime:
                          description: Optionally override the `expires_at` claim
                            in a JWT to enforce a custom expiration time on a token.
                            Equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-auth%20%3Cauthenticator%20name%3E.max_validity_time
                          format: int32
                          type: integer
                        name:
                          description: The name of the authenticator in Zuul's configuration,
                            see https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-auth%20%3Cauthenticator%20name%3E
                          type: string
                        realm:
                          description: Authentication realm, equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-auth%20%3Cauthenticator%20name%3E.realm
                          type: string
                        scope:
                          default: openid profile
                          description: The scope used to fetch a user's details, equivalent
                            to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-scope
                          type: string
                        skew:
                          default: 0
                          description: Optionally compensate for skew between Zuul's
                            and the Identity Provider's clocks, equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-auth%20%3Cauthenticator%20name%3E.skew
                          format: int32
                          type: integer
                        uidClaim:
                          default: sub
                          description: The JWT claim to use as a unique identifier
                            in audit logs, equivalent to https://zuul-ci.org/docs/zuul/latest/configuration.html#attr-auth%20%3Cauthenticator%20name%3E.issuer_id
                          type: string
                      required:
                      - clientID
                      - issuerID
                      - name
                      - realm
                      type: object
                    type: array
                  scheduler:
                    description: Configuration of the scheduler microservice
                    properties:
                      logLevel:
                        description: 'Specify the Log Level of the zuul-scheduler
                          service. Valid values are: - "INFO" (default) - "WARN" -
                          "DEBUG" Changing this value will restart the service.'
                        enum:
                        - INFO
                        - WARN
                        - DEBUG
                        type: string
                      statsdTarget:
                        description: The address to forward statsd metrics to (optional),
                          in the form "host:port"
                        type: string
                      storage:
                        description: Storage-related settings
                        properties:
                          className:
                            description: Default storage class to use with Persistent
                              Volume Claims issued by this resource. Consult your
                              cluster's configuration to see what storage classes
                              are available and recommended for your use case.
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: 'Storage space to allocate to the resource,
                              expressed as a Quantity: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/'
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - size
                        type: object
                    type: object
                  web:
                    description: Configuration of the web microservice
                    properties:
                      logLevel:
                        description: 'Specify the Log Level of the zuul-web launcher
                          service. Valid values are: - "INFO" (default) - "WARN" -
                          "DEBUG" Changing this value will restart the service.'
                        enum:
                        - INFO
                        - WARN
                        - DEBUG
                        type: string
                    type: object
                type: object
            required:
            - fqdn
            type: object
          status:
            description: SoftwareFactoryStatus defines the observed state of SoftwareFactory
            properties:
              conditions:
                description: Information about ongoing or completed reconciliation
                  processes between the Log server spec and the observed state of
                  the cluster
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    \n type FooStatus struct{ // Represents the observations of a
                    foo's current state. // Known .status.conditions.type are: \"Available\",
                    \"Progressing\", and \"Degraded\" // +patchMergeKey=type // +patchStrategy=merge
                    // +listType=map // +listMapKey=type Conditions []metav1.Condition
                    `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\"
                    protobuf:\"bytes,1,rep,name=conditions\"` \n // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: The Generation of the related Custom Resource that was
                  last processed by the operator controller
                format: int64
                type: integer
              ready:
                description: The deployment status.
                type: boolean
              reconciledBy:
                description: The name of the operator handling this Custom Resource's
                  reconciliation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
