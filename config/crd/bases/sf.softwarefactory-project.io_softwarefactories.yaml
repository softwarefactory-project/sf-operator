---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: softwarefactories.sf.softwarefactory-project.io
spec:
  group: sf.softwarefactory-project.io
  names:
    kind: SoftwareFactory
    listKind: SoftwareFactoryList
    plural: softwarefactories
    shortNames:
    - sf
    singular: softwarefactory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: SoftwareFactory is the Schema for the softwarefactories API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SoftwareFactorySpec defines the desired state of SoftwareFactory
            properties:
              config-location:
                description: Config repository spec
                properties:
                  base-url:
                    description: Base URL of the code-review provider where the `Name`
                      can be fetch by Git
                    pattern: ^https?:\/\/.+$
                    type: string
                  name:
                    description: Name of the `config` repository where the SF config
                      workflow is applied
                    minLength: 1
                    type: string
                  zuul-connection-name:
                    description: Name of the Zuul connection where the `config` exists
                    minLength: 1
                    type: string
                required:
                - base-url
                - name
                - zuul-connection-name
                type: object
              fqdn:
                type: string
              gitserver:
                description: Git server spec
                properties:
                  storage:
                    properties:
                      className:
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: Storage shrinking is not supported
                          rule: self >= oldSelf
                    required:
                    - size
                    type: object
                type: object
              logserver:
                description: Logserver service spec
                properties:
                  loopDelay:
                    description: Logs Check. Log will be checked every "X" seconds
                      ( default 3600 s ~= 1 hour )
                    type: integer
                  retentionDays:
                    description: Logs Older that "x" days will be purge ( default
                      60 days )
                    type: integer
                  storage:
                    properties:
                      className:
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: Storage shrinking is not supported
                          rule: self >= oldSelf
                    required:
                    - size
                    type: object
                type: object
                x-kubernetes-validations:
                - message: retentionDays must be a positive integer if set
                  rule: '!has(self.retentionDays) || self.retentionDays > 0'
                - message: loopDelay must be a positive integer if set
                  rule: '!has(self.loopDelay) || self.loopDelay > 0'
              mariadb:
                description: MariaDB service spec
                properties:
                  dbStorage:
                    properties:
                      className:
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: Storage shrinking is not supported
                          rule: self >= oldSelf
                    required:
                    - size
                    type: object
                  logStorage:
                    properties:
                      className:
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: Storage shrinking is not supported
                          rule: self >= oldSelf
                    required:
                    - size
                    type: object
                required:
                - dbStorage
                - logStorage
                type: object
              nodepool:
                description: Nodepool services spec
                properties:
                  launcher:
                    properties:
                      logLevel:
                        description: 'Specify the Log Level of the nodepool launcher
                          process. Valid values are: - "INFO" (default) - "WARN" -
                          "DEBUG"'
                        enum:
                        - INFO
                        - WARN
                        - DEBUG
                        type: string
                    type: object
                type: object
              storageClassName:
                description: Default storage class to use by Persistent Volume Claims
                type: string
              zookeeper:
                description: Zookeeper service spec
                properties:
                  storage:
                    properties:
                      className:
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: Storage shrinking is not supported
                          rule: self >= oldSelf
                    required:
                    - size
                    type: object
                required:
                - storage
                type: object
              zuul:
                description: Zuul service spec
                properties:
                  executor:
                    properties:
                      replicas:
                        format: int32
                        type: integer
                      storage:
                        properties:
                          className:
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                            x-kubernetes-validations:
                            - message: Storage shrinking is not supported
                              rule: self >= oldSelf
                        required:
                        - size
                        type: object
                    type: object
                  gerritconns:
                    items:
                      properties:
                        canonicalhostname:
                          description: The canonical hostname associated with the
                            git repos on the Gerrit server.
                          type: string
                        git-over-ssh:
                          default: false
                          description: This forces git operation over SSH even if
                            the password attribute is set.
                          type: boolean
                        hostname:
                          type: string
                        name:
                          type: string
                        password:
                          description: API Password secret name
                          type: string
                        port:
                          default: 29418
                          description: SSH port number to connect on the Gerrit instance
                          type: integer
                        puburl:
                          description: URL to Gerrit web interface
                          pattern: ^https?:\/\/.+$
                          type: string
                        username:
                          default: zuul
                          description: Username to authenticate on the Gerrit instance
                          type: string
                        verifyssl:
                          default: true
                          description: Disable SSL certificate verification when set
                            to false.
                          type: boolean
                      required:
                      - hostname
                      - name
                      type: object
                    type: array
                  scheduler:
                    properties:
                      storage:
                        properties:
                          className:
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                            x-kubernetes-validations:
                            - message: Storage shrinking is not supported
                              rule: self >= oldSelf
                        required:
                        - size
                        type: object
                    type: object
                type: object
            required:
            - fqdn
            type: object
          status:
            description: SoftwareFactoryStatus defines the observed state of SoftwareFactory
            properties:
              observedGeneration:
                format: int64
                type: integer
              ready:
                description: The deployment status.
                type: boolean
              reconciledBy:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
