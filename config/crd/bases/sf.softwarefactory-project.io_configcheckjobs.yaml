---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: configcheckjobs.sf.softwarefactory-project.io
spec:
  group: sf.softwarefactory-project.io
  names:
    kind: ConfigCheckJob
    listKind: ConfigCheckJobList
    plural: configcheckjobs
    shortNames:
    - ccj
    singular: configcheckjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: ConfigCheckJob is the Schema for the configcheckjobs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConfigCheckJobSpec defines the desired state of ConfigCheckJob
            properties:
              ttl:
                default: 600
                format: int32
                maximum: 86400
                minimum: 60
                type: integer
              zuulTenantConfig:
                minLength: 1
                type: string
            required:
            - zuulTenantConfig
            type: object
          status:
            description: ConfigCheckJobStatus defines the observed state of ConfigCheckJob
            properties:
              completionTime:
                format: date-time
                type: string
              outcome:
                type: string
              podID:
                type: string
              ready:
                type: boolean
              startTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
