apiVersion: sf.softwarefactory-project.io/v1
kind: SoftwareFactory
metadata:
  name: my-sf
spec:
  fqdn: "sftests.com"
  gerrit:
    enabled: true
    sshd_max_connections_per_user: "64"
  zuul:
    enabled: true
    gerritconns: []
