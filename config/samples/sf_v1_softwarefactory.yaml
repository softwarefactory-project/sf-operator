apiVersion: sf.softwarefactory-project.io/v1
kind: SoftwareFactory
metadata:
  name: my-sf
spec:
  fqdn: "sftests.com"
  etherpad: false
  lodgeit: false
  gerrit:
    enabled: true
    sshd_max_connections_per_user: "64"
  opensearch: true
  zuul:
    enabled: true
    gerritconns: []
