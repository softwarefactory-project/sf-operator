- project:
    check:
      jobs:
        - sf-operator-olm
        - sf-operator-upgrade
        - sf-operator-standalone
        - sf-operator-ansible-lint
    gate:
      jobs:
        - sf-operator-olm
        - sf-operator-upgrade
        - sf-operator-standalone
        - sf-operator-ansible-lint
    release:
      jobs:
        - sf-operator-publish-olm-bundle-image
