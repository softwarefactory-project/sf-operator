name: sf
tenant:
 name: local
 projects:
   - source: opendev.org
     config:
       - zuul/zuul-base-jobs
     untrusted:
       - zuul/zuul-jobs
components:
  mqtt: true
  test-config: true
connections: {}
